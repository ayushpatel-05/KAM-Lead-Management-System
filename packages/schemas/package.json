{
    "name": "@repo/schemas",
    "description": "Schemas and types commonly required accross the monorepo",
    "version": "1.0.0",
    "private": true,
    "license": "MIT",
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "publishConfig": {
        "access": "public"
    },
    "scripts": {
        "build": "tsc",
        "clean": "rm -rf dist",
        "dev": "tsc -w",
        "lint": "eslint \"src/**/*.ts*\" --max-warnings 0",
        "test": "jest"
    },
    "dependencies": {
        "zod": "^3.24.1"
    },
    "devDependencies": {
        "typescript": "^5.7.2"
    }
}
